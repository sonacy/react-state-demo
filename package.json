{
  "name": "react-state-demo",
  "private": true,
  "scripts": {
    "start": "lerna run start --scope=@react-state-demo/frontend",
    "build": "lerna run build --scope={@react-state-demo/components,@react-state-demo/hooks-cache,@react-state-demo/resource}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": []
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}